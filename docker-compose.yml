services:
  myfinance_app:
    container_name: myfinance_app
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: True
    tty: True
    restart: on-failure
    ports:
      - "8001:8001"
    volumes:
      - .:/app
    env_file:
      - envs/.env
